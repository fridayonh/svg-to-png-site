{
  "name": "svg-to-png-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "PORT=6006 node server.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
